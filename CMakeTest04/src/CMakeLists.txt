# 指定cmake最低编译版本
CMAKE_MINIMUM_REQUIRED(VERSION 3.10.2)
#指定头文件目录位置
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include)

AUX_SOURCE_DIRECTORY(. DIR_TEST1_SRCS)
ADD_LIBRARY(Test ${DIR_TEST1_SRCS})  # 生成静态库